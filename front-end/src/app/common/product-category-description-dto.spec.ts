import { ProductCategoryDescriptionDto } from './product-category-description-dto';

describe('ProductCategoryDescriptionDto', () => {
  it('should create an instance', () => {
    expect(new ProductCategoryDescriptionDto()).toBeTruthy();
  });
});
