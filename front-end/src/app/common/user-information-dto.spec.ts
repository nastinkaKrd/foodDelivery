import { UserInformationDto } from './user-information-dto';

describe('UserInformationDto', () => {
  it('should create an instance', () => {
    expect(new UserInformationDto()).toBeTruthy();
  });
});
