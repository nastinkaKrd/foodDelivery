import { AddingProductData } from './adding-product-data';

describe('AddingProductData', () => {
  it('should create an instance', () => {
    expect(new AddingProductData()).toBeTruthy();
  });
});
