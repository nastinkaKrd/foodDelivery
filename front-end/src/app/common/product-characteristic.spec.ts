import { ProductCharacteristic } from './product-characteristic';

describe('ProductCharacteristic', () => {
  it('should create an instance', () => {
    expect(new ProductCharacteristic()).toBeTruthy();
  });
});
