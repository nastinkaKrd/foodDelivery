import { ProductMemoryValueData } from './product-memory-value-data';

describe('ProductMemoryValueData', () => {
  it('should create an instance', () => {
    expect(new ProductMemoryValueData()).toBeTruthy();
  });
});
